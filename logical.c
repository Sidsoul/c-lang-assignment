// program of logical operators
#include <stdio.h>

int main()
{

    int a = 20, b = 4;

    // using and
    printf("a && b : %d\n", a && b);
   // using or
    printf("a || b : %d\n", a || b);
   // using not
    printf("!a: %d\n", !a);

    return 0;
}

